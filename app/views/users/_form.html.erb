<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :backup_email %><br />
    <%= f.text_field :backup_email %>
  </div>
	<div class="field">
		<%= f.label :domain %><br />
		<%= f.collection_select :domain_id, Domain.all, :id, :domain %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
